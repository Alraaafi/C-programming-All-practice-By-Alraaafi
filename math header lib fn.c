#include<stdio.h>
#include<math.h>
int main(){
    float x,sum;
    printf("entere x for equation:");
    scanf("%f",&x);
    sum = (4*x*x*x)-(6*x*x)+(7*x)-(10*x)+2;
    printf("solition is: %f",sum);
}
